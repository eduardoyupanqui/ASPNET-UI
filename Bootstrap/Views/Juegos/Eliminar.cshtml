@model Modelos.JuegosDTO

@{
    ViewBag.Title = "Eliminar";
    Layout = null;
}

<h2>Eliminar</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <dl class="dl-horizontal">
        @*<div class="row">
            <div class="col-md-2"></div>
            <div class="col-md-10"></div>
        </div>*@
        <dt>
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nombre)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Descripcion)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Descripcion)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.GenerosDTO.Nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.GenerosDTO.Nombre)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ConsolasDTO.Nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ConsolasDTO.Nombre)
        </dd>

        <dt></dt>
        <dd>
            @if (Model.ListaImagenes != null && Model.ListaImagenes.Any())
            {
                var imagen = Model.ListaImagenes.FirstOrDefault(i=>i.EsPortada);

                if (imagen!=null)
                {
                    <img src="@Url.Content(imagen.UrlImagenMiniatura)" alt="@imagen.Id"/>
                }
            }
        </dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        
        <div class="form-actions no-color">

            <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    <button id="btnEliminar" type="submit" class="btn btn-primary">Eliminar</button>
            </div>
        </div>
        
    }
</div>
